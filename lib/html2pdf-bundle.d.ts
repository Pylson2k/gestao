/** Bundle oficial com html2canvas + jspdf embutidos (subpath não tipado no pacote). */
declare module 'html2pdf.js/dist/html2pdf.bundle.min.js' {
  const html2pdf: () => unknown
  export default html2pdf
}
